# Public backend API base URL compiled into the Next.js client bundle.
# Use NEXT_PUBLIC_* because login links and static deployments need this at build time.
NEXT_PUBLIC_BACKEND_BASE_URL=http://localhost:8000
